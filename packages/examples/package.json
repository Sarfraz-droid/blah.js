{
  "name": "examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tailwind-watch": "tailwindcss -i ./input.css -o ./output.css --watch",
    "serve": "serve",
    "cy:open": "cypress open",
    "cy:run": "cypress run --headless --browser chrome",
    "test": "start-server-and-test serve 3000 cy:run"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "cypress": "^12.10.0",
    "postcss": "^8.4.23",
    "serve": "^14.2.0",
    "start-server-and-test": "^2.0.0",
    "tailwindcss": "^3.3.1"
  }
}
